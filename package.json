{
  "name": "cylinderdicer",
  "private": true,
  "type": "module",
  "scripts": {
    "convex:dev": "convex dev",
    "convex:codegen": "convex codegen",
    "convex:typecheck": "tsc --noEmit --target ES2022 --module ESNext --moduleResolution Bundler --strict --skipLibCheck convex/**/*.ts"
  },
  "dependencies": {
    "convex": "^1.42.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
