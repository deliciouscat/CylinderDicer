{
  "name": "cylinderdicer",
  "private": true,
  "type": "module",
  "scripts": {
    "convex:dev": "convex dev",
    "convex:codegen": "convex codegen",
    "convex:typecheck": "tsc --noEmit --target ES2022 --module ESNext --moduleResolution Bundler --strict --skipLibCheck convex/**/*.ts",
    "convex:domain-test": "rm -rf .tmp/convex-domain && tsc --outDir .tmp/convex-domain --target ES2022 --module CommonJS --moduleResolution Node --ignoreDeprecations 6.0 --strict --skipLibCheck convex/match/*.ts convex/protocol/*.ts && node -e \"require('fs').writeFileSync('.tmp/convex-domain/package.json', JSON.stringify({ type: 'commonjs' }))\" && node --test convex/match/tests/*.test.mjs",
    "phase0:test": "npm run convex:typecheck && npm run convex:domain-test && (cd web && npm run build) && luajit play/game/model/test_runner.lua",
    "phase1:check": "node scripts/convex-phase1-check.mjs",
    "phase1:bootstrap": "node scripts/convex-phase1-bootstrap.mjs"
  },
  "dependencies": {
    "convex": "^1.42.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
