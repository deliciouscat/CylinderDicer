# CylinderDicer Web

Vue.js web shell for account, lobby, shop, ranking, inventory, and the Defold play wrapper.

The Defold HTML5 build should be copied into `web/public/play/`, then loaded by `src/play-wrapper/DefoldCanvas.vue`.
